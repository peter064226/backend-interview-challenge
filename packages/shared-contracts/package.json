{
    "name": "shared-contracts",
    "version": "1.0.0",
    "private": true,
    "description": "Shared contracts and types across applications",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "clean": "rm -rf dist"
    },
    "devDependencies": {
        "typescript": "^5.3.0"
    }
}